{
  "name": "figma-spritesheet-creator",
  "version": "1.0.0",
  "description": "plugin for generating spritesheet and corresponding CSS code",
  "main": "code.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "npm run build -- --watch"
  },
  "author": "riya09",
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "typescript": "*"
  }
}
